beir>=2.0.0
colbert-ai>=0.2.19
torch>=2.0.0
opensearch-py>=2.4.0
numpy>=1.24.0
tqdm>=4.65.0

# IRPAPERS benchmark (ColModernVBERT)
datasets>=2.14.0
Pillow>=9.0.0
# colpali-engine: install from git for ColModernVBERT support
# git clone https://github.com/illuin-tech/colpali.git && cd colpali && git checkout vbert && pip install -e .
